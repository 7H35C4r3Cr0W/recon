# HANDOFF — read the local working handoff first

> **Claude Code / anyone continuing this project:** the real, detailed working handoff is kept
> **on the maintainer's local box, off the public repo.** Read it first — it has the current repo
> state, the owner's rules, how the work is done, what's been done, and what to do next.
>
> ### → `HANDOFF.local.md`
> (this repo folder, on the maintainer's machine — **gitignored, never committed**).
> Backup copy: `~/nabu-private/removed-docs/HANDOFF.md`.

If `HANDOFF.local.md` is **not present** (e.g. a fresh clone on another machine), do **not** proceed
from guesses — ask the maintainer for it. `CLAUDE.md` remains the public source of truth for the hard
rules (§2 exam-legality especially).

*This pointer is intentionally the only handoff file in the public repo — the working notes stay local
so they can hold machine paths, session detail, and WIP without publishing them.*
