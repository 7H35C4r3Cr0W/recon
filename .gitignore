# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# Tool caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Coverage
.coverage
.coverage.*
htmlcov/
coverage.xml

# Generated logs
*.log

# Local-only helper artifacts (not project assets)
mirror.sh

# Runtime workspace (profiles live at ~/oscprecon/, never in the repo)
/oscprecon/

# Secrets / credentials — never commit
*.pem
*.key
*.ppk
id_rsa*
id_ed25519*
*.ovpn
.env
.env.*
creds.json
*_creds.json
*.kdbx

# Walkthroughs — third-party writeups stay local (CLAUDE.md §21)
walkthroughs/*
!walkthroughs/README.md
!walkthroughs/_sample-*.md

# Local-only helper / backup scripts (personal infra — not project assets)
autosave.sh
mirror.sh
